# Note: This project now uses GitHub Actions workflows for CI/CD
# See .github/workflows/ directory for the active build and test workflows
# This AppVeyor configuration is kept for reference but may be deprecated

image: Visual Studio 2017

cache:
  - packages -> **\packages.config

configuration: Debug

build:
  parallel: true
  project: 

before_build:
  - nuget restore

environment:
  access_token: "UPDATE_ME"
